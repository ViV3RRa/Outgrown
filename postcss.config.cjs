// Tailwind CSS v4: use the consolidated @tailwindcss/postcss plugin.
// Autoprefixer is bundled; only add other plugins if truly needed.
module.exports = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
